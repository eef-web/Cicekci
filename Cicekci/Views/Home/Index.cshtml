@{
    ViewData["Title"] = "Anasayfa";
    var content = ViewBag.Content as Cicekci.Models.SiteContent ?? new Cicekci.Models.SiteContent();
    var products = ViewBag.Products as List<Cicekci.Models.Product> ?? new List<Cicekci.Models.Product>();
}

<!-- Hero Section -->
<section class="hero-section text-white text-center" style="background-color: #2d3a27; position: relative; overflow: hidden; padding: 120px 0;">
    @if (!string.IsNullOrWhiteSpace(content.HomeHeroImageUrl))
    {
        <img src="@content.HomeHeroImageUrl" alt="" aria-hidden="true" style="position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover;" />
        <div aria-hidden="true" style="position: absolute; inset: 0; background: rgba(45,58,39,0.75);"></div>
    }
    <div class="container" style="position: relative;">
        <h1 class="display-4 fw-bold">@content.HomeHeroTitle</h1>
        <p class="lead mt-3">@content.HomeHeroSubtitle</p>
        <a href="/Products" class="btn btn-light btn-lg mt-3">Ürünleri Gör</a>
    </div>
</section>

<!-- Öne Çıkan Ürünler -->
<section class="py-5" style="background: #f8f9fa;">
    <div class="container">
        <h2 class="text-center mb-5" style="color: #2d3a27;">@content.FeaturedTitle</h2>
        <div class="row g-4">
            @foreach (var product in products)
            {
                <div class="col-md-4 col-lg-3">
                    <div class="card h-100 shadow-sm border-0" style="border-radius: 12px; overflow: hidden;">
                        @if (!string.IsNullOrEmpty(product.ImageUrl))
                        {
                            <img src="@product.ImageUrl" class="card-img-top" style="height: 220px; object-fit: cover;" />
                        }
                        <div class="card-body">
                            <h5 class="card-title">@product.Name</h5>
                            <p class="card-text text-muted small">@(product.Description.Length > 80 ? product.Description.Substring(0, 80) + "..." : product.Description)</p>
                            <div class="d-flex justify-content-between align-items-center">
                                <span class="fw-bold" style="color: #2d3a27;">@product.Price.ToString("N0") TL</span>
                                <a href="/Cart/Add?productId=@product.Id" class="btn btn-sm text-white" style="background: #2d3a27;">Sepete Ekle</a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
        <div class="text-center mt-4">
            <a href="/Products" class="btn btn-outline-success btn-lg">Tüm Ürünleri Gör</a>
        </div>
    </div>
</section>

<!-- İstatistikler -->
<section class="py-5 text-white text-center" style="background: #2d3a27;">
    <div class="container">
        <div class="row">
            <div class="col-md-4">
                <h2 class="fw-bold">@content.StatYears</h2>
                <p>Yıllık Tecrübe</p>
            </div>
            <div class="col-md-4">
                <h2 class="fw-bold">@content.StatCustomers</h2>
                <p>Mutlu Müşteri</p>
            </div>
            <div class="col-md-4">
                <h2 class="fw-bold">@content.StatProducts</h2>
                <p>Çiçek Çeşidi</p>
            </div>
        </div>
    </div>
</section>

<!-- Neden Biz? -->
<section class="py-5">
    <div class="container">
        <h2 class="text-center mb-5" style="color: #2d3a27;">@content.WhyTitle</h2>
        <div class="row g-4 text-center">
            <div class="col-md-3">
                <div class="p-4">
                    <i class="bi bi-truck" style="font-size: 3rem; color: #2d3a27;"></i>
                    <h4 class="mt-3">@content.Benefit1Title</h4>
                    <p class="text-muted">@content.Benefit1Description</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="p-4">
                    <i class="bi bi-flower1" style="font-size: 3rem; color: #2d3a27;"></i>
                    <h4 class="mt-3">@content.Benefit2Title</h4>
                    <p class="text-muted">@content.Benefit2Description</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="p-4">
                    <i class="bi bi-credit-card" style="font-size: 3rem; color: #2d3a27;"></i>
                    <h4 class="mt-3">@content.Benefit3Title</h4>
                    <p class="text-muted">@content.Benefit3Description</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="p-4">
                    <i class="bi bi-headset" style="font-size: 3rem; color: #2d3a27;"></i>
                    <h4 class="mt-3">@content.Benefit4Title</h4>
                    <p class="text-muted">@content.Benefit4Description</p>
                </div>
            </div>
        </div>
    </div>
</section>
